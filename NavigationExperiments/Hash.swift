//extension AppFeature.Path.State: Hashable {
//  func hash(into hasher: inout Hasher) {
//    switch self {
//    case .featureA(let featureA):
//      hasher.combine(0)
//      hasher.combine(featureA)
//    case .featureB(let featureB):
//      hasher.combine(1)
//      hasher.combine(featureB)
//    case .featureC(let featureC):
//      hasher.combine(2)
//      hasher.combine(featureC)
//    }
//  }
//}
